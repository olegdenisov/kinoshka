/**
 * Границы UI/drag слайдера — отдельные от `YEAR_RANGE_MIN`/`MAX` (1874/2050) в
 * `filtersToParams.ts`, которые кодируют собственный фолбэк открытого диапазона API. Любое
 * значение, которое может выдать этот слайдер, и так укладывается внутрь тех границ.
 *
 * Вынесены в отдельный модуль (а не экспортированы прямо из `YearRangeSlider.tsx`), потому что
 * `react/only-export-components` (`allowConstantExport`) допускает только литеральные константы
 * в файле компонента — `YEAR_SLIDER_MAX` вычисляется вызовом `new Date().getFullYear()` в
 * момент импорта модуля (не хардкодится), чтобы слайдер никогда не отставал от реальности, и
 * это уже не проходит как "литерал".
 */
export const YEAR_SLIDER_MIN = 1900
export const YEAR_SLIDER_MAX = new Date().getFullYear()
