// Канонический жанр = ровно то, что ожидает API-фильтр `genres.name` (RU-название).
// Без `id`/`slug` — ни один сценарий их не использует (YAGNI, см. план
// docs/plans/20260815-dynamic-genre-dictionary.md); при появлении реального
// потребителя тип расширяется отдельным изменением.
export type Genre = {
  name: string
}
