// Префикс aria-label аватара профиля (`ProfileAvatar`, `@features/profile`) — общая константа, а
// не два независимых литерала. `sentry.ts` (`@app`) скрабит PII из breadcrumb/span-текста по
// этому же префиксу (`scrubProfileNameFromText`); если бы префикс дублировался как отдельный
// литерал в обоих местах, будущая правка формулировки в `ProfileAvatar` без парного изменения в
// `sentry.ts` молча сломала бы скраб — ничего (компилятор, линт, тест) не поймало бы рассинхрон.
// `@shared` — единственный слой, легально видимый и из `@features/profile` (вниз), и из `@app`
// (вниз же, `@app` стоит над `pages` и может импортировать что угодно ниже по FSD-направлению).
export const PROFILE_ARIA_LABEL_PREFIX = 'Your profile: '
